<h3 class="w-25 bg-light text-dark text-center">注文履歴詳細</h3>

<h6><strong>注文情報</strong></h6>

<table>
  <tr>
    <th>注文日</th>
    <td><%= @order.created_at.strftime('%Y/%m/%d') %></td>
  </tr>
  <tr>
    <th>配送先</th>
    <td>
      <%= "〒" + @order.post_code %><br>
      <%= @order.address %><br>
      <%= @order.name %>
    </td>
  </tr>
  <tr>
    <th>支払方法</th>
    <td>
      <%= @order.payment_method_i18n %>
    </td>
  </tr>
  <tr>
    <th>ステータス</th>
    <td><%= @order.status_i18n %></td>
  </tr>
</table>

<h6><strong>請求情報</strong></h6>
<table>
  <tr>
    <th>商品合計</th>
    <td><%= @order.total_price %></td>
  </tr>
  <tr>
    <th>配送料</th>
    <td><%= @order.shipping_cost %></td>
  </tr>
  <tr>
    <th><strong>ご請求額</strong></th>
    <td><%= @order.total_price + @order.shipping_cost %></td>
  </tr>
</table>

<h6><strong>注文内容</strong></h6>

<table>
  <thead>
    <tr>
      <th>商品</th>
      <th>単価(税込)</th>
      <th>個数</th>
      <th>小計</th>
    </tr>
  </thead>
  <tbody>
    <% @order_details.each do |order_detail| %>
    <tr>
      <td><%= order_detail.item.name %></td>
      <td><%= order_detail.item.with_tax_price %></td>
      <td><%= order_detail.quantity %></td>
      <td><%= order_detail.quantity * order_detail.item.price %></td>
    </tr>
    <% end %>
  </tbody>
</table>